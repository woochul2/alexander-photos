/**
 * 단위: rem
 */
const BREAKPOINTS = Object.freeze({
  sm: 37.5,
  lg: 61.25,
  xl: 75,
});

/**
 * 단위: rem
 */
const MAX_WIDTH = 87.5;

const KEY = {
  esc: 'Escape',
  arrowRight: 'ArrowRight',
  arrowLeft: 'ArrowLeft',
};

/**
 * 단위: s
 */
const TRANSITION_DURATION = 0.3;

module.exports = {
  BREAKPOINTS,
  MAX_WIDTH,
  KEY,
  TRANSITION_DURATION,
};
